server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: wildfly_logs
    static_configs:
      - targets: [localhost]
        labels:
          job: wildfly
          __path__: /wildfly/logs/server.log